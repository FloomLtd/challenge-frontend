/** @jsx jsx */

import { jsx, css } from '@emotion/core';

const containerCss = css `
  display: flex;
  background: rgb(244, 220, 185);
  height:  
`

function Item() {
  return (
    <div>

    </div>
  );
}

export default Item;
